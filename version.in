package main

var PackageBugreport string = "@PACKAGE_BUGREPORT@" // uwe@ohse.de
var PackageIssues string = "@PACKAGE_ISSUES@" // that github page
var PackageName string = "@PACKAGE_NAME@" // streamedit
var PackageVersion string = "@PACKAGE_VERSION@" // streamedit-0.13.0-alpha
var PackageURL string = "@PACKAGE_URL@" // https://ohse.de and so on
var Build string // the linker sets this from git head

